# Language-translator
this is a language translator using python
It uses tkinter for GUI
also it has a googletrans library for doing translations

for installing googletranslator use:
 pip install googletrans
